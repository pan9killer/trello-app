import styles from "../../../styles/Home.module.scss";

const AddColumn = () => {
  return (
    <div className={styles.bracket__new}>
      <p>+ Add new column</p>
    </div>
  );
};

export default AddColumn;
